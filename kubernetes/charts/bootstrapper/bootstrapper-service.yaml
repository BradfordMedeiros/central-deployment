apiVersion: v1
kind: Service
metadata:
  name: bootstrapper-service
  labels: 
    app: bootstrapper
spec:
  type: NodePort
  ports:
  - name: bootstrapper
    port: 8000
    targetPort: 8000
  selector:
    app: bootstrapper
